# zabbixserver デプロイ 運用手順書

最終更新: 2026-07-09
関連ドキュメント: [msp-frontend-server/msp_frontend_design.md](../msp-frontend-server/msp_frontend_design.md)

## 前提となる構成

- 対象VM: `msp-frontend-server`（GCP, project: msp-zabbix, zone: asia-northeast1-a, Debian 12）
- 接続: `gcloud compute ssh msp-frontend-server --zone=asia-northeast1-a --tunnel-through-iap`
- リポジトリ: `https://github.com/yoshihisahagisaka/atlib-msp-dev`（private）
- VM上のクローン先: `/opt/atlib-msp-dev`（sparse-checkoutで`zabbixserver/`のみ取得、他サービスのコードは置かない）
- デプロイ用SSH鍵: `~/.ssh/deploy_atlib_msp_dev`（GitHub側にRead-only Deploy Keyとして登録済み、SSH configのエイリアス名`github-atlib-deploy`）
- 本番反映先:
  - `/opt/msp-batch/batch/`：`main.py` `zabbix_client.py` `setup_alerts.py` `setup_discovery.py` `cpe_mapper.py` `nvd_client.py` `eol_client.py` `requirements.txt` `cpe_overrides.json` `eol_overrides.json` `config.yaml` を、`/opt/atlib-msp-dev/zabbixserver/batch/`配下の同名ファイルへのシンボリックリンクとして配置（`reports/`・`__pycache__`等の実行時生成物はリンク化しない・git管理外のまま）
  - `/var/www/html/atlib_monthly_report.html`：`/opt/atlib-msp-dev/zabbixserver/atlib_monthly_report.html`へのシンボリックリンク（`/var/www/html/`配下の他ファイル＝`api-bridge.php`等には触れない）

## 手順1: 通常のデプロイ（2回目以降）

コード変更をコミット・pushしたあと、VM上で以下を実行するだけでよい。シンボリックリンク経由のため、コピー・サービス再起動は不要。

```bash
gcloud compute ssh msp-frontend-server --zone=asia-northeast1-a --tunnel-through-iap --command="cd /opt/atlib-msp-dev && git pull"
```

反映確認（任意）:

```bash
# Pythonバッチの構文確認
cd /opt/msp-batch/batch && sudo /opt/msp-batch/venv/bin/python3 main.py --dry-run

# Webページの反映確認
curl -sk https://localhost/atlib_monthly_report.html -H 'Host: topology.zabbix.atlib.jp' -o /dev/null -w 'HTTP %{http_code}\n'
```

## 手順2: 初回セットアップ（別マシンに立て直す場合等）

1. VMにgitをインストール: `sudo apt-get install -y git`
2. デプロイ専用SSH鍵を生成: `ssh-keygen -t ed25519 -f ~/.ssh/deploy_atlib_msp_dev -N ""`
3. 公開鍵（`~/.ssh/deploy_atlib_msp_dev.pub`）を、GitHubリポジトリの Settings > Deploy keys（`https://github.com/yoshihisahagisaka/atlib-msp-dev/settings/keys`）へ**Read-onlyで**登録する
4. `~/.ssh/config`に以下を追記:
   ```
   Host github-atlib-deploy
     HostName github.com
     User git
     IdentityFile ~/.ssh/deploy_atlib_msp_dev
     IdentitiesOnly yes
   ```
5. sparse-checkoutでクローン:
   ```bash
   sudo mkdir -p /opt/atlib-msp-dev && sudo chown "$(whoami)":"$(whoami)" /opt/atlib-msp-dev
   cd /opt/atlib-msp-dev
   git clone --filter=blob:none --no-checkout git@github-atlib-deploy:yoshihisahagisaka/atlib-msp-dev.git .
   git sparse-checkout init --cone
   git sparse-checkout set zabbixserver
   git checkout main
   ```
6. 本番配置とのシンボリックリンクを作成（「前提となる構成」の一覧を参照。`sudo ln -sf /opt/atlib-msp-dev/zabbixserver/batch/<file> /opt/msp-batch/batch/<file>` を対象ファイル分繰り返す）

## トラブルシューティング

### `git pull`が認証エラーになる

Deploy Keyが失効・削除されていないかGitHub側の設定を確認する。鍵を再生成する場合は手順2の2〜4をやり直す。

### `main.py --dry-run`が権限エラーになる

`/opt/msp-batch/batch/reports/`ディレクトリの所有者が、実行しているOSユーザーと一致しているか確認する（このVMには`hagisaka`と`YoshihisaHagisaka`という別ユーザーが混在しているため、実行ユーザーによって権限エラーになることがある）。日次バッチは`/etc/msp-batch/env`（root:root）を読む必要があるため、実運用では`sudo`（root権限）での実行を前提とする。

### `/etc/msp-batch/env`のパスワードが古い

`atladmin`のパスワードはGCP Secret Manager（`zabbix-api-password`、project: msp-zabbix）で管理されている。`/etc/msp-batch/env`のファイル自体は追従できていないため、実行時は以下でSecret Managerから直接取得して上書きする運用としている（恒久対応は別途検討）。

```bash
TOKEN=$(curl -s -H "Metadata-Flavor: Google" \
  "http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/default/token" \
  | python3 -c 'import sys,json;print(json.load(sys.stdin)["access_token"])')
export ZABBIX_PASSWORD=$(curl -s -H "Authorization: Bearer ${TOKEN}" \
  "https://secretmanager.googleapis.com/v1/projects/msp-zabbix/secrets/zabbix-api-password/versions/latest:access" \
  | python3 -c 'import sys,json,base64; d=json.load(sys.stdin); print(base64.b64decode(d["payload"]["data"]).decode())')
```

## 変更履歴

- 2026-07-09: 初版作成。手動scpによるデプロイから、sparse-checkout + シンボリックリンクによる`git pull`ベースのデプロイ体制に移行。
